# Examples

This directory is reserved for non-executable examples of semantic artifacts,
governance decisions, ontology extensions, and model profiles.

Examples must not imply a schema language, implementation API, runtime
service, or storage model.

