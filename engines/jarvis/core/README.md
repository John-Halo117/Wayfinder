# Jarvis Core

Core contains durable navigation logic, route selection rules, state machines,
planning workflows, and recovery behavior.

Core must depend on contracts and services through explicit interfaces.

