# Internal CLI

Wayfinder-owned CLI surfaces live here. CLI modules call engines and services
through explicit APIs and do not own engine behavior.

## Commands

- `wf.py`: local wrapper for the ARK universal ingestion MVP.
