# Identity Model

Wayfinder owns identity semantics.

This directory is reserved for identity model placeholders. Polaris may own
machine security roots, and each repository enforces at its boundary, but
Wayfinder owns the semantic model.

